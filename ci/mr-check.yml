# To set up this pipeline, assuming you have an esback target locally setup, run:
# $ fly -t esback set-pipeline -p mr-check -c ci/mr-check.yml -v initial_version=0.0.0
---
resource_types:
  - name: merge-request
    type: docker-image
    source:
      repository: harbor-repo.vmware.com/tmc_concourse_tools/gitlab-merge-request-resource
      tag: latest
resources:
  - name: version
    type: semver
    icon: counter
    source:
      driver: s3
      bucket: esback.ci
      key: build/esback/version/mrcheck
      initial_version: ((initial_version))
      access_key_id: ((aws.deprecated_access_key))
      secret_access_key: ((aws.deprecated_access_secret))
  - name: gitlab-mr
    type: merge-request
    icon: git
    source:
      uri: https://gitlab.eng.vmware.com/esback/core.git
      private_token: ((gitlab.core_token))
  - name: mr-image
    type: registry-image
    icon: docker
    source:
      repository: harbor-repo.vmware.com/esback/internal-instance
      username: ((harbor.username))
      password: ((harbor.token))
  # Integration tests tools
  - name: ytt
    type: github-release
    icon: github
    source:
      owner: vmware-tanzu
      repository: carvel-ytt
      access_token: ((github.access_token))
  - name: cypress-image
    type: registry-image
    icon: docker
    source:
      repository: harbor-repo.vmware.com/dockerhub-proxy-cache/cypress/included
      tag: 8.5.0
  - name: bitbucket-image
    type: registry-image
    icon: docker
    source:
      repository: harbor-repo.vmware.com/dockerhub-proxy-cache/atlassian/bitbucket
      tag: 8.4.2

gitlab_pipeline_failure: &gitlab_pipeline_failure
  put: gitlab-mr
  params:
    repository: gitlab-mr
    status: failed

jobs:
  - name: mark-mr-running
    public: true
    plan:
      - get: gitlab-mr
        trigger: true
        version: every
      - put: gitlab-mr
        params:
          repository: gitlab-mr
          status: running
    on_failure: *gitlab_pipeline_failure
  - name: lint-and-test
    plan:
      - get: code
        resource: gitlab-mr
        passed: [mark-mr-running]
        trigger: true
      - in_parallel:
          - task: lint
            attempts: 2
            file: code/ci/lint.yml
          - task: test
            attempts: 2
            file: code/ci/unit-test.yml
    on_failure: *gitlab_pipeline_failure
  - name: bump
    plan:
      - get: gitlab-mr
        passed: [lint-and-test]
        trigger: true
      - put: version
        params:
          pre: mrcheck
    on_failure: *gitlab_pipeline_failure
  - name: build
    plan:
      - in_parallel:
          - do:
              - get: gitlab-mr
                passed: [bump]
                trigger: true
              - load_var: mrdata
                file: gitlab-mr/.git/merge-request.json
                reveal: true
          - do:
              - get: version
                passed: [bump]
                trigger: true
              - load_var: version
                file: version/version
                reveal: true
      - task: yarn-build
        input_mapping:
          code: gitlab-mr
        file: gitlab-mr/ci/yarn-build.yml
      - task: image-build
        privileged: true
        file: gitlab-mr/ci/image-build.yml
      - put: mr-image
        params:
          image: image/image.tar
          version: ((.:version))-((.:mrdata.source_branch))
    on_failure: *gitlab_pipeline_failure
  - name: integration-tests
    plan:
      - in_parallel:
          - get: ytt
          - get: cypress-image
            params: { format: oci }
          - get: bitbucket-image
            params: { format: oci }
          - do:
              - get: gitlab-mr
                passed: [build]
                trigger: true
              - load_var: mrdata
                file: gitlab-mr/.git/merge-request.json
                reveal: true
          - get: mr-image
            params: { format: oci }
            passed: [build]
            trigger: true
      - task: test
        privileged: true
        file: gitlab-mr/ci/integration-tests.yml
        input_mapping:
          code: gitlab-mr
          docker-image: mr-image
        vars:
          gitlab_token: ((gitlab.fixtures_token))
          git_branch: ((.:mrdata.source_branch))
          github_token: ((e2e.github_token))
          github_enterprise_token: ((e2e.github_enterprise_token))
          auth0_client_id: ((auth0.client_id))
          auth0_client_secret: ((auth0.client_secret))
          auth0_domain: ((auth0.domain))
          bitbucket_server_license: ((e2e.bitbucket_server_license))
          bitbucket_client_id: ((bitbucket.client_id))
          bitbucket_client_secret: ((bitbucket.client_secret))
          gke_control_plane_endpoint: ((gke.control_plane_endpoint))
          gke_app_live_view_endpoint: ((gke.app_live_view_endpoint))
          gke_service_account_token: ((gke.service_account_token))
          gke_oidc_control_plane_endpoint: ((gke_oidc.control_plane_endpoint))
          gke_oidc_client_id: ((gke_oidc.client_id))
          gke_oidc_client_secret: ((gke_oidc.client_secret))
          okta_client_id: ((okta.client_id))
          okta_client_secret: ((okta.client_secret))
          okta_audience: ((okta.audience))
          auth0_refresh_token: ((e2e.auth0_refresh_token))
          bitbucket_john_doe_refresh_token: ((e2e.bitbucket_john_doe_refresh_token))
          google_user_a_refresh_token: ((e2e.google_user_a_refresh_token))
          okta_refresh_token: ((e2e.okta_refresh_token))
          github_app_client_id: ((e2e.github_app_client_id))
          github_app_client_secret: ((e2e.github_app_client_secret))
          github_user_refresh_token: ((e2e.github_user_refresh_token))
    on_failure: *gitlab_pipeline_failure
  - name: mark-mr-success
    public: true
    plan:
      - get: gitlab-mr
        passed: [lint-and-test, build, integration-tests]
        trigger: true
      - put: gitlab-mr
        params:
          repository: gitlab-mr
          status: success
